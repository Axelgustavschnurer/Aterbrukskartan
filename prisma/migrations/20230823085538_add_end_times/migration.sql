BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[recycle] ADD [end_month] INT,
[end_year] INT;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
